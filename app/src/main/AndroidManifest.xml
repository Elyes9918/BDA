<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ma.ensaf.bda">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BDA">
        <activity android:name=".Chat.Activities.ReceiverProfile"></activity>
        <activity
            android:name=".Chat.Activities.ChatActivity"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity android:name=".Chat.Activities.MainChatActivity" />
        <activity android:name=".MainApp.CategorySelectedActivity" />
        <activity android:name=".MainApp.ProfileActivity" />
        <activity android:name=".MainApp.MainActivity" />
        <activity android:name=".Registration.RecipientRegistrationActivity" />
        <activity android:name=".Registration.DonarRegistrationActivity" />
        <activity android:name=".Registration.SelectRegesitrationActivity" />
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".Login.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".Chat.Firebase.MessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>